{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Current File (Conda)",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      },
      "conda": "py38"
    }
  ]
}
